import 'package:flutter/material.dart';

class DrinkList extends StatelessWidget {
  const DrinkList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Drink List Page'),
    );
  }
}
